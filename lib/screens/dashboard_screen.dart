import 'package:flutter/material.dart';
// import '../services/mock_api.dart'; // Jenny usará esto para las gráficas

class DashboardScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Dashboard de KPIs (Espacio de Jenny)'));
  }
}
